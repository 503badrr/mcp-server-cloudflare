# Agent Platform Validation

This repository uses CI as the merge gate for the Switch VS Code/Codespaces agent platform. A change is ready for merge only after dependency, type/lint, formatting, test, worker-build, and security checks complete successfully. Production deployment remains a separate approval step.
